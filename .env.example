# HTTP server port
PORT=3000

# SQLite database directory
DATA_DIR=./data

# Comma-separated allowed CORS origins (empty = no CORS headers)
CORS_ORIGINS=

# Pino log level: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# Runtime environment
NODE_ENV=development
